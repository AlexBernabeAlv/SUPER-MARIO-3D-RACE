using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreateKoopaTroopa : MonoBehaviour
{
    public GameObject koopaTroopa;

    // Start is called before the first frame update
    void Start()
    {
        //for (int i = 0; i < 12; ++i)
        //{
        //    GameObject obj;
        //    if (i % 2 == 0) obj = (GameObject)Instantiate(koopaTroopa, new Vector3(-3.0f, 1.0f, 30.0f +i), koopaTroopa.transform.rotation);
        //    else obj = (GameObject)Instantiate(koopaTroopa, new Vector3(3.0f, 1.0f, 30.0f+i), koopaTroopa.transform.rotation);
        //    obj.transform.parent = transform;
        //}
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
